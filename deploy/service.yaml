apiVersion: v1
kind: Service
metadata:
  name: viewer-backend
  namespace: sealos-storage-manager
spec:
  selector:
    app.kubernetes.io/name: viewer-backend
  ports:
    - name: http
      port: 80
      targetPort: http
