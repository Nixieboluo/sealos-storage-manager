# Encore SDK

The generated backend client now lives in the workspace package at
`packages/encore-client/src/generated/client.ts`:

```bash
pnpm generate:api
```

The generated `client.ts` file is intentionally ignored and regenerated by
`pnpm prepare`. Keep domain wrappers in feature-level `api/` folders.
